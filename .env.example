# Backend URL for contact form + Typing PRO (no trailing slash).
# Local dev:
VITE_API_BASE=http://localhost:3001
# GitHub Pages production build (replace with your Render Web Service URL, then: npm run build && npm run deploy):
# VITE_API_BASE=https://your-service.onrender.com
